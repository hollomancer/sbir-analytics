# Phase III Survivor Substantiation Audit — Requirements

**Target epistemic tier:** `pipelines`

This specification defines the Phase III census hand-label survivor substantiation audit.
The estimand, design, and decision boundaries are defined in [design.md](design.md).

Note: tier declared as `pipelines` because the spec is not yet frozen. Promotion to
`evidence` requires a frozen spec, SHA enforcement, blocking asset checks, and a declared
estimand — all four per CLAUDE.md. Promotion is explicit work after freeze.
