# Core ETL library

This directory contains shared source readers, identity primitives, models,
transformers, validation, configuration, and utility code. It supplies the
reusable primitives and pipelines used by studies and applications.

Library tests establish implementation behavior. They do not authorize a
research claim. Claim authority comes from a versioned study contract and its
frozen artifacts.
