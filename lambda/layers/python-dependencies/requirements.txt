# Lambda layer dependencies for lightweight functions
# Note: pandas/numpy removed - caused import errors in Lambda environment
# Functions needing heavy data processing should use Dagster Cloud instead
requests>=2.31.0
