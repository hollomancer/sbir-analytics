name: Weekly SBIR Awards Report

on:
  schedule:
    # Every Monday at 12 PM UTC (after ETL pipeline at 10 AM processes refreshed data)
    - cron: "0 12 * * 1"
  workflow_dispatch:
    inputs:
      days:
        description: "Number of days to look back"
        required: false
        default: "7"
        type: string
      skip_sbir_api:
        description: "Skip SBIR.gov API calls (set to true when API is down)"
        required: false
        default: "true"
        type: boolean
      debug:
        description: "Enable verbose debug output"
        required: false
        default: "false"
        type: boolean

permissions:
  contents: read

concurrency:
  group: weekly-awards-report
  cancel-in-progress: true

jobs:
  generate-report:
    name: Generate Weekly Awards Report
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6

      - name: Setup Python and UV
        uses: ./.github/actions/setup-python-uv
        with:
          python-version: "3.11"
          install-dev-deps: "false"

      - name: Generate weekly awards report
        env:
          DAYS: ${{ github.event.inputs.days || '7' }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          USPTO_ODP_API_KEY: ${{ secrets.USPTO_ODP_API_KEY }}
          SAM_GOV_API_KEY: ${{ secrets.SAM_GOV_API_KEY }}
          S3_BUCKET: ${{ secrets.S3_BUCKET || '' }}
          SKIP_SBIR_API: ${{ github.event.inputs.skip_sbir_api || 'true' }}
        run: |
          mkdir -p reports
          DEBUG_FLAG=""
          if [ "${{ github.event.inputs.debug }}" = "true" ]; then
            DEBUG_FLAG="--debug"
          fi
          uv run --frozen python scripts/data/weekly_awards_report.py \
            --days "$DAYS" \
            $DEBUG_FLAG \
            --output reports/weekly-awards.md

      - name: Post report to job summary
        if: always()
        run: |
          MAX_SIZE=55000  # GitHub step summary limit is ~1 MiB; stay well under
          if [ -f reports/weekly-awards.md ]; then
            SIZE=$(wc -c < reports/weekly-awards.md)
            if [ "$SIZE" -gt "$MAX_SIZE" ]; then
              head -c "$MAX_SIZE" reports/weekly-awards.md >> $GITHUB_STEP_SUMMARY
              echo "" >> $GITHUB_STEP_SUMMARY
              echo "---" >> $GITHUB_STEP_SUMMARY
              echo "*Report truncated (${SIZE} bytes). Full report available in the artifact.*" >> $GITHUB_STEP_SUMMARY
            else
              cat reports/weekly-awards.md >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "## Weekly Awards Report" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "Report generation failed. Check logs for details." >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload report artifact
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: weekly-awards-report-${{ github.run_id }}
          path: reports/weekly-awards.md
          retention-days: 30
