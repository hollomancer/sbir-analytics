# Transition report area configs
#
# One YAML per cet_id (must match config/cet/taxonomy.yaml).
# See specs/tech-area-transition-report/design.md for schema.
#
# Required: cet_id, display_name
# Optional: keyword_pack, cpc_prefixes, external_reference, sector_registries, audience
