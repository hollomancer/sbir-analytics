# SBIR.gov bulk award linkage fixture

`linkage_shape.csv` is synthetic and follows the reviewed `award_data.csv` column schema. It covers
four linkage shapes: NSF solicitation plus topic, NSF topic-only historical data, DoD solicitation
plus topic, and DoD solicitation without a topic. Its values are test inputs, not research evidence.
